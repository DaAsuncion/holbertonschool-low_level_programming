#include "main.h"
#include <stdio.h>

/**
 *_isupper - Write a function that checks
 *for uppercase character.
 * @c:1 if c is uppercase or 0 otherwise
 * Return: Always 0
 */
int _isupper(int c)
{
char c = 'A';
if (isupper(c))
return (1);
else
return (0);
}
